import React, { Component } from 'react';

class Neighborhoods extends Component {

    render() {
      return (
        <div className='main'>Neighborhoods</div>
      );
    }
}

export default Neighborhoods;
